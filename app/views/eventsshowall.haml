.container
  .content
    %h2 Events
    %table.table.table-striped
      %thead
        %tr
          %th ID
          %th Timestamp
          %th Type
          %th Beschreibung
	  %th Message
          %th Tags
        %tbody
      ~@events.each do |event|
        %tr
          %td= event['title']
          %td= event['title']
          %td= event['title']
          %td= event['title']
          %td= event['title']
          %td= event['title']
