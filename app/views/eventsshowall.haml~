.container
  .content
    %h2 Events
    %table.table.table-striped
      %thead
        %tr
          %th ID
          %th Timestamp
          %th Beschreibung
          %th Type
        %tbody
      ~@events.each do |event|
        %tr
          %td= event['_id']
          %td= event['_type']
          %td= event['title']
          %td= event.to_s
       
