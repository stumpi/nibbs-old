.container
  .content
    %h2 Events
    %table.table.table-striped
      %thead
        %tr
          %th Timestamp
          %th Type
          %th Message
        %tbody
      ~@events.each do |event|
        %tr
          %td= event['_id']
          %td= event['_id']
          %td= event['_id']
          %td= event['_id']

